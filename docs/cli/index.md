# CLI Package

`@thermal-label/labelmanager-cli` provides a thin command-line wrapper over the Node driver.

It is useful for:

- local testing
- quick ad-hoc labels
- scripting and cron jobs
- Linux setup onboarding

Install:

```bash
pnpm add -D @thermal-label/labelmanager-cli
```

Then run commands via `dymo ...`.
