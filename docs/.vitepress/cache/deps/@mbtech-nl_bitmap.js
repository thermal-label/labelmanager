// node_modules/.pnpm/@mbtech-nl+bitmap@1.0.1/node_modules/@mbtech-nl/bitmap/dist/font/font8x8.js
var FONT_8X8_ROWS = [
  [0, 0, 0, 0, 0, 0, 0, 0],
  // 0x20
  [24, 60, 60, 24, 24, 0, 24, 0],
  // 0x21
  [54, 54, 0, 0, 0, 0, 0, 0],
  // 0x22
  [54, 54, 127, 54, 127, 54, 54, 0],
  // 0x23
  [12, 62, 3, 30, 48, 31, 12, 0],
  // 0x24
  [0, 99, 51, 24, 12, 102, 99, 0],
  // 0x25
  [28, 54, 28, 110, 59, 51, 110, 0],
  // 0x26
  [6, 6, 3, 0, 0, 0, 0, 0],
  // 0x27
  [24, 12, 6, 6, 6, 12, 24, 0],
  // 0x28
  [6, 12, 24, 24, 24, 12, 6, 0],
  // 0x29
  [0, 102, 60, 255, 60, 102, 0, 0],
  // 0x2A
  [0, 12, 12, 63, 12, 12, 0, 0],
  // 0x2B
  [0, 0, 0, 0, 0, 12, 12, 6],
  // 0x2C
  [0, 0, 0, 63, 0, 0, 0, 0],
  // 0x2D
  [0, 0, 0, 0, 0, 12, 12, 0],
  // 0x2E
  [96, 48, 24, 12, 6, 3, 1, 0],
  // 0x2F
  [62, 99, 115, 123, 111, 103, 62, 0],
  // 0x30
  [12, 14, 12, 12, 12, 12, 63, 0],
  // 0x31
  [30, 51, 48, 28, 6, 51, 63, 0],
  // 0x32
  [30, 51, 48, 28, 48, 51, 30, 0],
  // 0x33
  [56, 60, 54, 51, 127, 48, 120, 0],
  // 0x34
  [63, 3, 31, 48, 48, 51, 30, 0],
  // 0x35
  [28, 6, 3, 31, 51, 51, 30, 0],
  // 0x36
  [63, 51, 48, 24, 12, 12, 12, 0],
  // 0x37
  [30, 51, 51, 30, 51, 51, 30, 0],
  // 0x38
  [30, 51, 51, 62, 48, 24, 14, 0],
  // 0x39
  [0, 12, 12, 0, 0, 12, 12, 0],
  // 0x3A
  [0, 12, 12, 0, 0, 12, 12, 6],
  // 0x3B
  [24, 12, 6, 3, 6, 12, 24, 0],
  // 0x3C
  [0, 0, 63, 0, 0, 63, 0, 0],
  // 0x3D
  [6, 12, 24, 48, 24, 12, 6, 0],
  // 0x3E
  [30, 51, 48, 24, 12, 0, 12, 0],
  // 0x3F
  [62, 99, 123, 123, 123, 3, 30, 0],
  // 0x40
  [12, 30, 51, 51, 63, 51, 51, 0],
  // 0x41
  [63, 102, 102, 62, 102, 102, 63, 0],
  // 0x42
  [60, 102, 3, 3, 3, 102, 60, 0],
  // 0x43
  [31, 54, 102, 102, 102, 54, 31, 0],
  // 0x44
  [127, 70, 22, 30, 22, 70, 127, 0],
  // 0x45
  [127, 70, 22, 30, 22, 6, 15, 0],
  // 0x46
  [60, 102, 3, 3, 115, 102, 124, 0],
  // 0x47
  [51, 51, 51, 63, 51, 51, 51, 0],
  // 0x48
  [30, 12, 12, 12, 12, 12, 30, 0],
  // 0x49
  [120, 48, 48, 48, 51, 51, 30, 0],
  // 0x4A
  [103, 102, 54, 30, 54, 102, 103, 0],
  // 0x4B
  [15, 6, 6, 6, 70, 102, 127, 0],
  // 0x4C
  [99, 119, 127, 127, 107, 99, 99, 0],
  // 0x4D
  [99, 103, 111, 123, 115, 99, 99, 0],
  // 0x4E
  [28, 54, 99, 99, 99, 54, 28, 0],
  // 0x4F
  [63, 102, 102, 62, 6, 6, 15, 0],
  // 0x50
  [30, 51, 51, 51, 59, 30, 56, 0],
  // 0x51
  [63, 102, 102, 62, 54, 102, 103, 0],
  // 0x52
  [30, 51, 7, 14, 56, 51, 30, 0],
  // 0x53
  [63, 45, 12, 12, 12, 12, 30, 0],
  // 0x54
  [51, 51, 51, 51, 51, 51, 63, 0],
  // 0x55
  [51, 51, 51, 51, 51, 30, 12, 0],
  // 0x56
  [99, 99, 99, 107, 127, 119, 99, 0],
  // 0x57
  [99, 99, 54, 28, 28, 54, 99, 0],
  // 0x58
  [51, 51, 51, 30, 12, 12, 30, 0],
  // 0x59
  [127, 99, 49, 24, 76, 102, 127, 0],
  // 0x5A
  [30, 6, 6, 6, 6, 6, 30, 0],
  // 0x5B
  [3, 6, 12, 24, 48, 96, 64, 0],
  // 0x5C
  [30, 24, 24, 24, 24, 24, 30, 0],
  // 0x5D
  [8, 28, 54, 99, 0, 0, 0, 0],
  // 0x5E
  [0, 0, 0, 0, 0, 0, 0, 255],
  // 0x5F
  [12, 12, 24, 0, 0, 0, 0, 0],
  // 0x60
  [0, 0, 30, 48, 62, 51, 110, 0],
  // 0x61
  [7, 6, 6, 62, 102, 102, 59, 0],
  // 0x62
  [0, 0, 30, 51, 3, 51, 30, 0],
  // 0x63
  [56, 48, 48, 62, 51, 51, 110, 0],
  // 0x64
  [0, 0, 30, 51, 63, 3, 30, 0],
  // 0x65
  [28, 54, 6, 15, 6, 6, 15, 0],
  // 0x66
  [0, 0, 110, 51, 51, 62, 48, 31],
  // 0x67
  [7, 6, 54, 110, 102, 102, 103, 0],
  // 0x68
  [12, 0, 14, 12, 12, 12, 30, 0],
  // 0x69
  [48, 0, 48, 48, 48, 51, 51, 30],
  // 0x6A
  [7, 6, 102, 54, 30, 54, 103, 0],
  // 0x6B
  [14, 12, 12, 12, 12, 12, 30, 0],
  // 0x6C
  [0, 0, 51, 127, 127, 107, 99, 0],
  // 0x6D
  [0, 0, 31, 51, 51, 51, 51, 0],
  // 0x6E
  [0, 0, 30, 51, 51, 51, 30, 0],
  // 0x6F
  [0, 0, 59, 102, 102, 62, 6, 15],
  // 0x70
  [0, 0, 110, 51, 51, 62, 48, 120],
  // 0x71
  [0, 0, 59, 110, 102, 6, 15, 0],
  // 0x72
  [0, 0, 62, 3, 30, 48, 31, 0],
  // 0x73
  [8, 12, 62, 12, 12, 44, 24, 0],
  // 0x74
  [0, 0, 51, 51, 51, 51, 110, 0],
  // 0x75
  [0, 0, 51, 51, 51, 30, 12, 0],
  // 0x76
  [0, 0, 99, 107, 127, 127, 54, 0],
  // 0x77
  [0, 0, 99, 54, 28, 54, 99, 0],
  // 0x78
  [0, 0, 51, 51, 51, 62, 48, 31],
  // 0x79
  [0, 0, 63, 25, 12, 38, 63, 0],
  // 0x7A
  [56, 12, 12, 7, 12, 12, 56, 0],
  // 0x7B
  [24, 24, 24, 0, 24, 24, 24, 0],
  // 0x7C
  [7, 12, 12, 56, 12, 12, 7, 0],
  // 0x7D
  [110, 59, 0, 0, 0, 0, 0, 0],
  // 0x7E
  [0, 0, 0, 0, 0, 0, 0, 0],
  // 0x7F (DEL)
];
var FONT_8X8 = new Uint8Array(FONT_8X8_ROWS.flat());
var FONT_8X8_METRICS = {
  charWidth: 8,
  charHeight: 8,
};

// node_modules/.pnpm/@mbtech-nl+bitmap@1.0.1/node_modules/@mbtech-nl/bitmap/dist/font/render.js
var FONTS = {
  default8x8: { data: FONT_8X8, metrics: FONT_8X8_METRICS },
};
function assertScale(name, value) {
  if (!Number.isInteger(value) || value < 1) {
    throw new RangeError(`${name} must be a positive integer`);
  }
}
function assertSpacing(value) {
  if (!Number.isInteger(value) || value < 0) {
    throw new RangeError('letterSpacing must be a non-negative integer');
  }
}
function maskTrailingBits(data, widthPx, heightPx) {
  const trailingBits = widthPx % 8;
  if (trailingBits === 0) {
    return;
  }
  const rowBytes = Math.ceil(widthPx / 8);
  const mask = (255 << (8 - trailingBits)) & 255;
  for (let y = 0; y < heightPx; y += 1) {
    const idx = y * rowBytes + rowBytes - 1;
    data[idx] &= mask;
  }
}
function renderText(text, options = {}) {
  if (text.length === 0) {
    throw new RangeError('text must not be empty');
  }
  const {
    font = 'default8x8',
    scaleX = 1,
    scaleY = 1,
    letterSpacing = 1,
    invert = false,
  } = options;
  assertScale('scaleX', scaleX);
  assertScale('scaleY', scaleY);
  assertSpacing(letterSpacing);
  const { data: fontData, metrics } = FONTS[font];
  const chars = Array.from(text).map(c => {
    const code = c.codePointAt(0) ?? 32;
    return code >= 32 && code <= 127 ? code : 32;
  });
  const baseWidth = chars.length * metrics.charWidth + (chars.length - 1) * letterSpacing;
  const widthPx = baseWidth * scaleX;
  const heightPx = metrics.charHeight * scaleY;
  const rowBytes = Math.ceil(widthPx / 8);
  const data = new Uint8Array(heightPx * rowBytes);
  for (let charIndex = 0; charIndex < chars.length; charIndex += 1) {
    const charCode = chars[charIndex];
    const glyphOffset = (charCode - 32) * metrics.charHeight;
    const charX = charIndex * (metrics.charWidth + letterSpacing);
    for (let row = 0; row < metrics.charHeight; row += 1) {
      const glyphRow = fontData[glyphOffset + row];
      for (let col = 0; col < metrics.charWidth; col += 1) {
        const on = (glyphRow >> col) & 1;
        if (on === 0) {
          continue;
        }
        for (let sy = 0; sy < scaleY; sy += 1) {
          for (let sx = 0; sx < scaleX; sx += 1) {
            const x = (charX + col) * scaleX + sx;
            const y = row * scaleY + sy;
            const idx = y * rowBytes + Math.floor(x / 8);
            data[idx] |= 1 << (7 - (x % 8));
          }
        }
      }
    }
  }
  if (invert) {
    for (let i = 0; i < data.length; i += 1) {
      data[i] = ~data[i] & 255;
    }
    maskTrailingBits(data, widthPx, heightPx);
  }
  return { widthPx, heightPx, data };
}
function measureText(text, options = {}) {
  const { font = 'default8x8', scaleX = 1, scaleY = 1, letterSpacing = 1 } = options;
  assertScale('scaleX', scaleX);
  assertScale('scaleY', scaleY);
  assertSpacing(letterSpacing);
  const { metrics } = FONTS[font];
  const baseWidth =
    text.length === 0 ? 0 : text.length * metrics.charWidth + (text.length - 1) * letterSpacing;
  return { widthPx: baseWidth * scaleX, heightPx: metrics.charHeight * scaleY };
}

// node_modules/.pnpm/@mbtech-nl+bitmap@1.0.1/node_modules/@mbtech-nl/bitmap/dist/dither.js
function floydSteinberg(luminance, width, height, invert) {
  const pixels = new Float32Array(luminance);
  const rowBytes = Math.ceil(width / 8);
  const data = new Uint8Array(height * rowBytes);
  for (let y = 0; y < height; y += 1) {
    for (let x = 0; x < width; x += 1) {
      const idx = y * width + x;
      const oldPixel = pixels[idx];
      const newPixel = oldPixel < 0.5 ? 0 : 1;
      pixels[idx] = newPixel;
      const error = oldPixel - newPixel;
      if (x + 1 < width) {
        pixels[idx + 1] += error * (7 / 16);
      }
      if (y + 1 < height) {
        if (x > 0) {
          pixels[idx + width - 1] += error * (3 / 16);
        }
        pixels[idx + width] += error * (5 / 16);
        if (x + 1 < width) {
          pixels[idx + width + 1] += error * (1 / 16);
        }
      }
      const isBlack = invert ? newPixel === 1 : newPixel === 0;
      if (isBlack) {
        const byteIndex = y * rowBytes + Math.floor(x / 8);
        data[byteIndex] |= 1 << (7 - (x % 8));
      }
    }
  }
  return { widthPx: width, heightPx: height, data };
}

// node_modules/.pnpm/@mbtech-nl+bitmap@1.0.1/node_modules/@mbtech-nl/bitmap/dist/transform.js
function getPixel(bitmap, x, y) {
  const rowBytes = Math.ceil(bitmap.widthPx / 8);
  const byteIdx = y * rowBytes + Math.floor(x / 8);
  return ((bitmap.data[byteIdx] >> (7 - (x % 8))) & 1) === 1;
}
function setPixel(data, rowBytes, x, y, black) {
  const byteIdx = y * rowBytes + Math.floor(x / 8);
  const bit = 1 << (7 - (x % 8));
  if (black) {
    data[byteIdx] |= bit;
  } else {
    data[byteIdx] &= ~bit;
  }
}
function rotateBitmap(bitmap, degrees) {
  const { widthPx: w, heightPx: h } = bitmap;
  if (degrees === 180) {
    const rowBytes2 = Math.ceil(w / 8);
    const data2 = new Uint8Array(h * rowBytes2);
    for (let y = 0; y < h; y += 1) {
      for (let x = 0; x < w; x += 1) {
        setPixel(data2, rowBytes2, w - 1 - x, h - 1 - y, getPixel(bitmap, x, y));
      }
    }
    return { widthPx: w, heightPx: h, data: data2 };
  }
  const newW = h;
  const newH = w;
  const rowBytes = Math.ceil(newW / 8);
  const data = new Uint8Array(newH * rowBytes);
  for (let y = 0; y < h; y += 1) {
    for (let x = 0; x < w; x += 1) {
      const black = getPixel(bitmap, x, y);
      if (degrees === 90) {
        setPixel(data, rowBytes, h - 1 - y, x, black);
      } else {
        setPixel(data, rowBytes, y, w - 1 - x, black);
      }
    }
  }
  return { widthPx: newW, heightPx: newH, data };
}
function flipHorizontal(bitmap) {
  const { widthPx: w, heightPx: h } = bitmap;
  const rowBytes = Math.ceil(w / 8);
  const data = new Uint8Array(h * rowBytes);
  for (let y = 0; y < h; y += 1) {
    for (let x = 0; x < w; x += 1) {
      setPixel(data, rowBytes, w - 1 - x, y, getPixel(bitmap, x, y));
    }
  }
  return { widthPx: w, heightPx: h, data };
}
function flipVertical(bitmap) {
  const { widthPx: w, heightPx: h } = bitmap;
  const rowBytes = Math.ceil(w / 8);
  const data = new Uint8Array(h * rowBytes);
  for (let y = 0; y < h; y += 1) {
    for (let x = 0; x < w; x += 1) {
      setPixel(data, rowBytes, x, h - 1 - y, getPixel(bitmap, x, y));
    }
  }
  return { widthPx: w, heightPx: h, data };
}
function invertBitmap(bitmap) {
  const { widthPx, heightPx } = bitmap;
  const rowBytes = Math.ceil(widthPx / 8);
  const data = new Uint8Array(bitmap.data);
  const trailingBits = widthPx % 8;
  const mask = trailingBits === 0 ? 255 : (255 << (8 - trailingBits)) & 255;
  for (let y = 0; y < heightPx; y += 1) {
    for (let b = 0; b < rowBytes; b += 1) {
      const idx = y * rowBytes + b;
      data[idx] = ~data[idx] & (b === rowBytes - 1 ? mask : 255);
    }
  }
  return { widthPx, heightPx, data };
}
function scaleBitmap(bitmap, targetHeight) {
  if (!Number.isInteger(targetHeight) || targetHeight < 1) {
    throw new RangeError('targetHeight must be a positive integer');
  }
  const srcW = bitmap.widthPx;
  const srcH = bitmap.heightPx;
  const targetWidth = Math.max(1, Math.round(srcW * (targetHeight / srcH)));
  const rowBytes = Math.ceil(targetWidth / 8);
  const data = new Uint8Array(targetHeight * rowBytes);
  for (let y = 0; y < targetHeight; y += 1) {
    const srcY = Math.floor((y * srcH) / targetHeight);
    for (let x = 0; x < targetWidth; x += 1) {
      const srcX = Math.floor((x * srcW) / targetWidth);
      setPixel(data, rowBytes, x, y, getPixel(bitmap, srcX, srcY));
    }
  }
  return { widthPx: targetWidth, heightPx: targetHeight, data };
}
function cropBitmap(bitmap, x, y, widthPx, heightPx) {
  if (x < 0 || y < 0) throw new RangeError('x and y must be non-negative');
  if (widthPx < 1 || heightPx < 1) throw new RangeError('widthPx and heightPx must be positive');
  if (x + widthPx > bitmap.widthPx || y + heightPx > bitmap.heightPx) {
    throw new RangeError('crop region out of bounds');
  }
  const rowBytes = Math.ceil(widthPx / 8);
  const data = new Uint8Array(heightPx * rowBytes);
  for (let cy = 0; cy < heightPx; cy += 1) {
    for (let cx = 0; cx < widthPx; cx += 1) {
      setPixel(data, rowBytes, cx, cy, getPixel(bitmap, x + cx, y + cy));
    }
  }
  return { widthPx, heightPx, data };
}
function stackBitmaps(bitmaps, direction) {
  if (bitmaps.length === 0) throw new RangeError('bitmaps must not be empty');
  if (bitmaps.length === 1) {
    const only = bitmaps[0];
    if (!only) throw new RangeError('bitmaps must not be empty');
    return { widthPx: only.widthPx, heightPx: only.heightPx, data: new Uint8Array(only.data) };
  }
  if (direction === 'horizontal') {
    const first2 = bitmaps[0];
    if (!first2) throw new RangeError('bitmaps must not be empty');
    const height = first2.heightPx;
    if (!bitmaps.every(b => b.heightPx === height))
      throw new RangeError('all heights must match for horizontal stack');
    const width2 = bitmaps.reduce((sum, b) => sum + b.widthPx, 0);
    const rowBytes2 = Math.ceil(width2 / 8);
    const data2 = new Uint8Array(height * rowBytes2);
    for (let y = 0; y < height; y += 1) {
      let offsetX = 0;
      for (const bmp of bitmaps) {
        for (let x = 0; x < bmp.widthPx; x += 1) {
          setPixel(data2, rowBytes2, offsetX + x, y, getPixel(bmp, x, y));
        }
        offsetX += bmp.widthPx;
      }
    }
    return { widthPx: width2, heightPx: height, data: data2 };
  }
  const first = bitmaps[0];
  if (!first) throw new RangeError('bitmaps must not be empty');
  const width = first.widthPx;
  if (!bitmaps.every(b => b.widthPx === width))
    throw new RangeError('all widths must match for vertical stack');
  const totalHeight = bitmaps.reduce((sum, b) => sum + b.heightPx, 0);
  const rowBytes = Math.ceil(width / 8);
  const data = new Uint8Array(totalHeight * rowBytes);
  let offsetY = 0;
  for (const bmp of bitmaps) {
    for (let y = 0; y < bmp.heightPx; y += 1) {
      for (let x = 0; x < width; x += 1) {
        setPixel(data, rowBytes, x, y + offsetY, getPixel(bmp, x, y));
      }
    }
    offsetY += bmp.heightPx;
  }
  return { widthPx: width, heightPx: totalHeight, data };
}
function padBitmap(bitmap, padding) {
  const top = padding.top ?? 0;
  const right = padding.right ?? 0;
  const bottom = padding.bottom ?? 0;
  const left = padding.left ?? 0;
  const newW = bitmap.widthPx + left + right;
  const newH = bitmap.heightPx + top + bottom;
  const rowBytes = Math.ceil(newW / 8);
  const data = new Uint8Array(newH * rowBytes);
  for (let y = 0; y < bitmap.heightPx; y += 1) {
    for (let x = 0; x < bitmap.widthPx; x += 1) {
      setPixel(data, rowBytes, x + left, y + top, getPixel(bitmap, x, y));
    }
  }
  return { widthPx: newW, heightPx: newH, data };
}

// node_modules/.pnpm/@mbtech-nl+bitmap@1.0.1/node_modules/@mbtech-nl/bitmap/dist/image.js
function renderImage(image, options = {}) {
  if (image.width === 0 || image.height === 0) {
    throw new RangeError('image dimensions must be non-zero');
  }
  if (image.data.length !== image.width * image.height * 4) {
    throw new RangeError('image.data length does not match dimensions');
  }
  const { threshold = 128, dither = false, invert = false, rotate = 0 } = options;
  const luminance = rgbaToLuminance(image);
  const result = dither
    ? floydSteinberg(luminance, image.width, image.height, invert)
    : thresholdToBitmap(luminance, image.width, image.height, threshold, invert);
  if (rotate === 0) {
    return result;
  }
  return rotateBitmap(result, rotate);
}
function rgbaToLuminance(image) {
  const luminance = new Float32Array(image.width * image.height);
  for (let i = 0; i < luminance.length; i += 1) {
    const r = image.data[i * 4];
    const g = image.data[i * 4 + 1];
    const b = image.data[i * 4 + 2];
    const a = image.data[i * 4 + 3] / 255;
    const lum = 0.2126 * r + 0.7152 * g + 0.0722 * b;
    const composited = (1 - a) * 255 + a * lum;
    luminance[i] = composited / 255;
  }
  return luminance;
}
function thresholdToBitmap(luminance, width, height, threshold, invert) {
  const rowBytes = Math.ceil(width / 8);
  const data = new Uint8Array(height * rowBytes);
  for (let y = 0; y < height; y += 1) {
    for (let x = 0; x < width; x += 1) {
      const idx = y * width + x;
      const dark = luminance[idx] * 255 < threshold;
      const isBlack = invert ? !dark : dark;
      if (isBlack) {
        const byteIdx = y * rowBytes + Math.floor(x / 8);
        data[byteIdx] |= 1 << (7 - (x % 8));
      }
    }
  }
  return { widthPx: width, heightPx: height, data };
}

// node_modules/.pnpm/@mbtech-nl+bitmap@1.0.1/node_modules/@mbtech-nl/bitmap/dist/encode.js
function getRow(bitmap, y) {
  if (y < 0 || y >= bitmap.heightPx) {
    throw new RangeError(`row ${String(y)} out of bounds`);
  }
  const rowBytes = bytesPerRow(bitmap.widthPx);
  return bitmap.data.slice(y * rowBytes, y * rowBytes + rowBytes);
}
function* iterRows(bitmap) {
  const rowBytes = bytesPerRow(bitmap.widthPx);
  for (let y = 0; y < bitmap.heightPx; y += 1) {
    yield bitmap.data.subarray(y * rowBytes, y * rowBytes + rowBytes);
  }
}
function bytesPerRow(widthPx) {
  return Math.ceil(widthPx / 8);
}
function createBitmap(widthPx, heightPx) {
  if (!Number.isInteger(widthPx) || widthPx < 1) {
    throw new RangeError('widthPx must be a positive integer');
  }
  if (!Number.isInteger(heightPx) || heightPx < 1) {
    throw new RangeError('heightPx must be a positive integer');
  }
  return {
    widthPx,
    heightPx,
    data: new Uint8Array(heightPx * bytesPerRow(widthPx)),
  };
}
function bitmapEquals(a, b) {
  if (a.widthPx !== b.widthPx || a.heightPx !== b.heightPx) {
    return false;
  }
  if (a.data.length !== b.data.length) {
    return false;
  }
  return a.data.every((byte, index) => {
    return byte === b.data[index];
  });
}
export {
  bitmapEquals,
  bytesPerRow,
  createBitmap,
  cropBitmap,
  flipHorizontal,
  flipVertical,
  floydSteinberg,
  getPixel,
  getRow,
  invertBitmap,
  iterRows,
  measureText,
  padBitmap,
  renderImage,
  renderText,
  rotateBitmap,
  scaleBitmap,
  stackBitmaps,
};
//# sourceMappingURL=@mbtech-nl_bitmap.js.map
