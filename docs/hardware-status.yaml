# Community-verified hardware status for @thermal-label/labelmanager.
# See https://github.com/thermal-label/.github/blob/main/CONTRIBUTING/hardware-status-schema.md
# for the canonical schema and validation rules.
#
# Devices in DEVICES that don't appear here are rendered as "untested"
# on https://thermal-label.github.io/hardware/. To add a verification
# report, file a Hardware verification issue and a maintainer will
# update this file.
#
# This file is validated on push by scripts/validate-hardware-status.mjs.

schemaVersion: 1
driver: labelmanager

devices:
  - pid: 0x1002
    name: LabelManager PnP
    status: verified
    transports:
      usb: verified
    lastVerified: 2026-04-27
    packageVersion: '0.2.0'
    quirks: |
      The LabelManager PnP and PC share USB PID `0x1002`. The PC variant
      typically boots into mass-storage mode first (`0x1001`) and needs
      `usb_modeswitch` on Linux. Once switched, both behave identically
      from the driver's perspective.
    notes: |
      Reference test device for the LabelManager family. Text and image
      printing confirmed on 6/9/12 mm tape; auto-feed cut works.
    reports:
      - issue: 0
        reporter: '@mannes'
        date: 2026-04-27
        result: verified
        os: Linux
        selfVerified: true
        notes: |
          Bench verification by the maintainer at the time of seeding
          this file. Issue #0 is a placeholder — replace with the real
          issue number when a public verification report is filed.
